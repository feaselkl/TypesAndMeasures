open System

[<EntryPoint>]
let main argv =
    printfn "If you are running this from Visual Studio or Visual Studio Code, please execute the contents of the script files instead!"
    0 // return an integer exit code